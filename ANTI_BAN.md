# Обход блокировок Avito

## Что улучшено в парсере

### 1. Случайные задержки
```python
# БЫЛО: фиксированные задержки
time.sleep(2)  # всегда 2 секунды
time.sleep(3)  # всегда 3 секунды

# СТАЛО: случайные задержки (имитация человека)
time.sleep(random.uniform(3, 6))  # 3-6 секунд
time.sleep(random.uniform(5, 10))  # 5-10 секунд между страницами
```

### 2. Имитация поведения человека
- Плавный скроллинг страницы (3 скролла по 300-500px)
- Случайные паузы между скроллами (0.5-1.5 сек)
- Логи показывают задержки: "Пауза 7.3 сек перед следующей страницей..."

### 3. Автоматическое обнаружение блокировок
```python
if "Доступ ограничен" in page_source:
    logger.error("❌ ОБНАРУЖЕНА БЛОКИРОВКА AVITO!")
    raise ConnectionError("IP заблокирован")
```

## Что делать если получили блокировку

### Сразу:
1. **Остановите парсинг** (бот автоматически остановится)
2. **Подождите 10-15 минут** (Avito снимет временный бан)

### Для получения нового IP:
3. **Перезагрузите роутер:**
   ```
   - Выключите роутер на 30 секунд
   - Включите обратно
   - Проверьте новый IP: https://2ip.ru/
   ```

4. **Или используйте VPN:**
   ```bash
   # Подключите любой VPN (NordVPN, ProtonVPN, и т.д.)
   # Выберите российский сервер (для доступа к Avito)
   ```

### Уменьшите нагрузку:
5. **Используйте меньше страниц:**
   ```bash
   # БЫЛО: парсим 10 страниц (риск бана)
   python run.py --query "видеокарта" --pages 10

   # СТАЛО: парсим по 1-2 страницы за раз
   python run.py --query "видеокарта" --pages 1 --limit 20
   ```

6. **Делайте паузы между запусками:**
   ```
   Запуск 1 → Пауза 5-10 минут → Запуск 2 → Пауза 5-10 минут → ...
   ```

## Рекомендуемый режим работы

**Безопасный режим (низкий риск бана):**
```bash
python run.py --query "ноутбук" --location "moskva" --pages 1 --limit 20 --headless
# Пауза 10 минут
python run.py --query "видеокарта" --location "sankt-peterburg" --pages 1 --limit 20 --headless
```

**Агрессивный режим (высокий риск бана):**
```bash
python run.py --query "iPhone" --pages 5 --limit 100 --headless
# Риск блокировки ~80%
```

## Признаки что скоро забанят

1. Страницы грузятся медленнее обычного
2. Появляются пустые результаты
3. Логи показывают таймауты
4. Появляется текст "Подтвердите что вы не робот"

**→ ОСТАНОВИТЕ ПАРСИНГ и сделайте паузу!**

## Технические детали

### Текущие задержки:
- После загрузки страницы: **3-6 секунд** (случайно)
- Между страницами: **5-10 секунд** (случайно)
- Скроллинг: **3 скролла × 0.5-1.5 сек = 1.5-4.5 сек**

### Итого на 1 страницу:
**Минимум:** 3 + 1.5 = 4.5 сек
**Максимум:** 6 + 4.5 = 10.5 сек
**Среднее:** ~7 секунд

### На 10 объявлений (1 страница):
~40-60 секунд (нормально)

### На 100 объявлений (5 страниц):
~5-8 минут (риск бана средний)

## Улучшения для будущего

1. **Прокси-ротация** (смена IP каждые N запросов)
2. **Решение капчи** (2captcha, anticaptcha)
3. **Headless браузеры** (puppeteer-extra с stealth plugin)
4. **Распределённый парсинг** (несколько машин с разными IP)

## Статистика блокировок

| Режим                | Объявлений | Риск бана |
|----------------------|-----------|-----------|
| 1 страница/10 мин    | ~20       | Низкий    |
| 3 страницы/10 мин    | ~60       | Средний   |
| 5+ страниц подряд    | 100+      | Высокий   |
| 10+ страниц          | 200+      | Почти 100%|

**Вывод:** Парсите по 1-2 страницы с паузами 5-10 минут между запусками.
